If you upgrade __APP__ from version `1.3.20~ynh1`, you should verify if some mails have been blocked in the queue. you can use this commands connected with ssh :

```
yunohost app shell __APP__
php8.5 bin/paheko queue count
php8.5 bin/paheko queue run --force
```
